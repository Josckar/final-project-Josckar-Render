<!DOCTYPE html>
<html>

<head>
    <title>Schedule Appointments</title>

</head>

<body>
    {{#if alreadyLoggedIn}}
    <div style="background-color: #4CAF50; color: black; padding: 10px; text-align: center;">
        You are already logged in!
    </div>
    {{/if}}
    <h2>Schedule Appointments</h2>
    <h3>If none of these slots work, please enter an appointment and we will contact you at our earliest convenience</h3>
    <form action="/appointments/schedule" method="POST">
        <label for="date">Date:</label>
        <input type="date" id="date" name="date" required>
        <br>
        <label for="time">Time:</label>
        <input type="time" id="time" name="time" required>
        <br>
        <button type="submit">Schedule Appointment</button>
    </form>
    <table class="appointments-table">
        <thead>
            <tr>
                <th>Appointment ID</th>
                <th>Date</th>
                <th>Time</th>
                <th>Customer</th>
                <th>Employee</th>
                <th>Action</th> <!-- Add a new column for the action -->
            </tr>
        </thead>
        <tbody>
            {{#each availableAppointments}}
            <tr>
                <td>{{ id }}</td>
                <td>{{ date }}</td>
                <td>{{ time }}</td>
                <td>{{ customer }}</td>
                <td>{{ employee }}</td>
                <td><a href="/accept-appointment/{{ id }}">Click to accept appointment</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>

</html>